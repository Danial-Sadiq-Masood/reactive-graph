# Reactive Graph Tasks

- [ ] Change calculated node dependees from `Set` to `Map` so calculations can access dependencies by name.
- [ ] Add reaction epochs so stale intermediate results can be ignored or discarded.
- [ ] Support different value types across nodes instead of one graph-wide generic `T`.
